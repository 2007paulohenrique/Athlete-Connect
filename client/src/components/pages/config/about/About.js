import styles from "./AboutConfig.module.css";

function About() {
    return (
        <section className={styles.about_config}>
            <h3>Athlete Connect</h3>
        </section>
    );
}

export default About;